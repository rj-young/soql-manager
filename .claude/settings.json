{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(npx tsc --noEmit *)",
      "Bash(yarn workspace soql-manager lint)",
      "Bash(yarn workspace soql-manager tsc --noEmit *)"
    ]
  }
}
